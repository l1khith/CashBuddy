# Models Directory

This directory stores quantized on-device ONNX models bundled as assets for PaisaPal KMP.

- `distilbert-transaction-classifier-quantized.onnx`: INT8 quantized DistilBERT transaction classification model (~10 MB disk, ~15 MB runtime memory).
